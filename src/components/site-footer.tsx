/** One quiet line: what Vetra is, and what its listing data is not. */
export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-line">
      <p className="container-app text-pretty py-5 text-[0.78rem] leading-relaxed text-fg-subtle">
        Vetra is an independent product decision prototype. Listing details were collected once and
        may have changed. Vetra does not sell or ship products.
      </p>
    </footer>
  );
}
